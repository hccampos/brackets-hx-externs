package brackets.command;

@:native("brackets_externs.command.ContextMenuIds")
extern class ContextMenuIds {
    private function new() { }

    public var EDITOR_MENU(default, never):String;
    public var INLINE_EDITOR_MENU(default, never):String;
    public var PROJECT_MENU(default, never):String;
    public var WORKING_SET_MENU(default, never):String;
}